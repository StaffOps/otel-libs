name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  dotnet:
    name: .NET ${{ matrix.dotnet }} (test + coverage)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        dotnet: ['8.0.x', '10.0.x']
    defaults:
      run:
        working-directory: dotnet
    env:
      # net8.0 test dll runs on the .NET 10 runtime via roll-forward
      DOTNET_ROLL_FORWARD: Major
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ matrix.dotnet }}
      - run: dotnet restore OtelHelper.Tests/OtelHelper.Tests.csproj
      - run: >
          dotnet test OtelHelper.Tests
          --configuration Release
          --no-restore
          /p:CollectCoverage=true
          /p:CoverletOutputFormat=cobertura
          /p:CoverletOutput=./coverage/
          /p:Threshold=90
          /p:ThresholdType=line
          /p:ThresholdStat=total
          /p:Exclude="[xunit*]*"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dotnet-coverage-${{ matrix.dotnet }}
          path: dotnet/OtelHelper.Tests/coverage/

  python:
    name: Python (test + coverage)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -e ".[dev]" grpcio -q
      - run: >
          pytest tests/ --ignore=tests/test_propagation.py -v
          --cov=otel_helper
          --cov-report=term-missing
          --cov-report=xml:coverage.xml
          --cov-fail-under=90
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: python-coverage
          path: python/coverage.xml

  example-images:
    name: Build example images (no push)
    runs-on: ubuntu-latest
    needs: [dotnet, python]
    strategy:
      fail-fast: false
      matrix:
        include:
          - context: dotnet
            dockerfile: dotnet/example/dotnet-api/Dockerfile
          - context: dotnet
            dockerfile: dotnet/example/dotnet-backend/Dockerfile
          - context: dotnet
            dockerfile: dotnet/example/dotnet-process/Dockerfile
          - context: python
            dockerfile: python/example/python-api/Dockerfile
          - context: python
            dockerfile: python/example/python-backend/Dockerfile
          - context: python
            dockerfile: python/example/python-process/Dockerfile
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
